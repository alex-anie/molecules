import { RouterProvider, createBrowserRouter, createRoutesFromElements, Route } from "react-router-dom";

export default function App() {
    return (
        <div className="text-red-500">This is a new project</div>
    )
}
